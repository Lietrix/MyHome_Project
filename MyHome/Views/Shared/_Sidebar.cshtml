@if (Request.IsAuthenticated)
{
<head>
    <style>
        bg-light
    </style>
    </head>

          //Sidebar
    <div class="bg-light border-right" id="sidebar-wrapper">
        <div class="sidebar-heading"> Menu </div>
        <div class="list-group list-group-flush">
            <a href="@Url.Action("Index", "Dashboard")" class="list-group-item list-group-item-action bg-light">Dashboard</a>
            <a href="@Url.Action("Index", "Items")" class="list-group-item list-group-item-action bg-light">All Items</a>
            <a href="@Url.Action("Index", "Manage")" class="list-group-item list-group-item-action bg-light">Profile</a>
        </div>
    </div>
}
else
{
    //do nothing for now
}
